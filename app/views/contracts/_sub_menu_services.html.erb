<ul>
   <% services.each do |service, children| %>

      <li>
        <%= link_to_unless_current service.name, service_path(service),
       class: "  #{ 'white' if service.ancestry != nil } " %>
      </li>
        <%= render 'sub_menu_services', services: children if children.present? %>
  <% end %>
</ul>