<div class="d-flex bg-dark">
  <%= render 'buttons/add_operator', service: @service,
  variable: Variable.new(name: "x",value: "*", operator:true,fixed: true,role: "value") %>
  <%= render 'buttons/add_operator', service: @service,
  variable: Variable.new(name: "/",value: "/", operator:true,fixed: true,role: "value") %>
  <%= render 'buttons/add_operator', service: @service,
  variable: Variable.new(name: "+",value: "+", operator:true,fixed: true,role: "value") %>
  <%= render 'buttons/add_operator', service: @service,
  variable: Variable.new(name: "-",value: "-", operator:true,fixed: true,role: "value") %>
  <%= render 'buttons/add_operator', service: @service,
  variable: Variable.new(name: "(",value: "(", operator:true,fixed: true,role: "value") %>
  <%= render 'buttons/add_operator', service: @service,
  variable: Variable.new(name: ")",value: ")", operator:true,fixed: true,role: "value") %>
</div>