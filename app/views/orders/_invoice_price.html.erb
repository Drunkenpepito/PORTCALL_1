<%= turbo_frame_tag "invoice_price_#{order.id}" do %>
    <%= ((order.calculate * ( 1 + order.taxes.where(isfee:true).sum(&:percentage)*0.01)).round(4)* ( 1 + order.taxes.where(isfee:false).sum(&:percentage)*0.01)).round(4) %>
<% end %>