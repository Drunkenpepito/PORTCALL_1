
<%= turbo_frame_tag "budget_prices_#{order.id}" do %>
    <% if order.children != [] && order.children.all?{|o| o.calculate_net.is_a? Numeric } %>
        <%= order.children.sum(&:calculate_net) %>
    <% else %>
        <% if order.calculate.is_a? Numeric  %>
            <%= render "orders/budget_price", order:order %>
        <% end %>
    <% end %>
<% end %>
