
<style> body { background-color: #ff7f50; } </style>

<div class=" d-flex align-items-center justify-content-center blue p-3 ">
  <span class="m-2 white fs-2 ">SUPPLIERS</span>
  <span class=" m-2 white fs-2 "><%= link_to '♼', update_list_suppliers_path, class: "light-green", style: "font-size: 40px;", data:{turbo_method: :patch} %></span>
</div>
<div class=" d-flex align-items-center justify-content-center blue p-3">
  <%= search_form_for @q do |f| %>
    <%# <%= f.label :name_or_contact_cont %> 
    <%= f.search_field :name_or_contact_cont %>
    <%= f.submit "🔎" %>
  <% end %>
</div>


<div class ="d-flex justify-content-center">
  <table class="table  table table-bordered table-hover border border-light border-2">
    <tbody>
      <% if @suppliers != nil %>
        <%= render @suppliers.sort_by{ |s| s.name } %>
      <% else %>
        NO SUPPLIER CAN BE EXTRACTED
      <% end %>
    </tbody>
  </table>
</div>
